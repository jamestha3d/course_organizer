import * as React from 'react';

interface ILessonsListProps {
}

const LessonsList: React.FunctionComponent<ILessonsListProps> = (props) => {
    return (
        <>
            <h2 className="mt-3"> Lessons</h2>
        </>
    );
};

export default LessonsList;
